# 复制本文件为 rag/.env 后填入自己的密钥。.env 不会被提交到仓库。
#
# ===== 大模型 (Generator) API 配置 =====
# 按 OpenAI 标准格式调用，任何兼容该格式的服务都可用：
# DeepSeek、OpenAI、通义、Kimi、本地 vLLM/Ollama 等，改 URL 与模型 ID 即可。
OPENAI_API_BASE_URL=https://api.deepseek.com/v1
OPENAI_API_KEY=sk-your-api-key
OPENAI_MODEL=deepseek-chat
# 可选：设置页面的模型下拉列表（格式：id1:显示名1,id2:显示名2）
# OPENAI_MODEL_LIST=deepseek-chat:DeepSeek-V3,deepseek-reasoner:DeepSeek-R1,gpt-4o:GPT-4o
#
# 从旧版本升级：DEEPSEEK_API_KEY 等旧变量名仍可用，程序会自动回退读取，
# 不必改动现有 .env。新旧同时存在时以 OPENAI_* 为准。

# ===== Embedding / Rerank API 配置 =====
# EMBED_API_KEY 同时供两处使用，缺了问答无法工作：
#   1. 向量检索：运行时把用户问题转成向量，去知识库找相关段落
#   2. 结果重排：把检索到的段落按与问题的相关度重新排序
# 服务商必须同时提供「向量模型」与「重排模型」（如硅基流动），
# 两者共用一个 key 与 base_url。
EMBED_API_BASE_URL=https://your-api-endpoint.example.com/v1
EMBED_API_KEY=sk-your-embedding-api-key
# EMBED_MODEL 必须保持默认值：仓库自带的索引由该模型生成，
# 换模型会使向量维度与语义空间不一致，检索结果完全错乱。
# EMBED_MODEL=Qwen/Qwen3-Embedding-0.6B
# RERANK_MODEL=Qwen/Qwen3-Reranker-4B

# ===== 检索调优 =====
# RERANK_TIMEOUT=30
# SCORE_THRESHOLD=0.0
#
# 说明：其余可调项（Top-K、温度、缓存上限、提示词文件等）统一存放在
# 项目根目录的 config.json，可在设置页面修改，无需手工编辑本文件。

# ===== 服务配置 =====
# 允许跨域访问的来源（逗号分隔）。
# 不配置时默认只放行 http://localhost:8000 与 http://127.0.0.1:8000。
# 前后端同源部署无需配置；若前端独立部署在其他域名下才需要设置。
# CORS_ALLOW_ORIGINS=http://localhost:5173,https://your-frontend.example.com
